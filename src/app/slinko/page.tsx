import { SlinkoGame } from '../../components/SlinkoGame';

export default function SlinkoPage() {
    return (
        <div className="h-[calc(100vh-56px)] w-full overflow-hidden bg-black">
            <SlinkoGame />
        </div>
    );
}

